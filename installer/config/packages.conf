#!/usr/bin/env bash
# List of pacman and AUR packages to install

# ------------------------
# Pacman packages
# ------------------------
PACMAN_PACKAGES=(
    pwvucontrol
    kdeconnect
    geany
    zip
    xarchiver
    audacious
    ttf-jetbrains-mono-nerd
    gnome-themes-extra
    sassc
    adw-gtk-theme
    flatpak
    gvfs
    ffmpegthumbnailer
    tumbler
    poppler-glib
    qt5ct
    qt6ct
    nwg-look
    kitty
    shfmt
    yq
    neovim
)

# ------------------------
# AUR packages
# ------------------------
AUR_PACKAGES=(
    thunar
    thunar-volman
    thunar-archive-plugin
    thunar-media-tags-plugin
)
